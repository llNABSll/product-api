name: Nightly SonarQube Scan

on:
  schedule:
    - cron: '0 0 * * *'  # Tous les jours à minuit UTC

jobs:
  sonarqube:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: sonarqube scan
        uses: llNABSll/nightly-action@main
        with:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}